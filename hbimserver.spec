# -*- mode: python -*-
a = Analysis(['hbimserver.py'],
             pathex=['C:\\Users\\Administrator\\Desktop\\hbimserver'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='hbimserver.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='hbimserver')
